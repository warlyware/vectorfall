{
  "name": "subspace-flight-lab",
  "version": "1.0.32",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build && node scripts/build-server.mjs",
    "build:server": "node scripts/build-server.mjs",
    "test": "node scripts/build-server.mjs && vitest run"
  },
  "dependencies": {
    "three": "^0.179.1"
  },
  "devDependencies": {
    "@types/three": "^0.179.0",
    "typescript": "^5.9.2",
    "vite": "^6.4.3",
    "vitest": "^3.2.4"
  }
}
